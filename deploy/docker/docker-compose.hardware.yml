# Overlay for scanning drives attached to the Docker host.
# Requires Linux, root in the API container, and access to block devices.
#
#   CDI_HEALTH_API_TOKEN=... docker compose -f deploy/docker/docker-compose.yml \
#     -f deploy/docker/docker-compose.hardware.yml up --build
#
# CDI_HEALTH_API_TOKEN is required (inherited from the base compose file).

services:
  api:
    user: "0:0"
    privileged: true
    environment:
      CDI_HEALTH_API_ALLOW_NON_ROOT: "0"
      CDI_HEALTH_API_MOCK_DATA: ""
      CDI_HEALTH_API_TOKEN: ${CDI_HEALTH_API_TOKEN:?Set CDI_HEALTH_API_TOKEN in .env}
    command:
      [
        "python",
        "-m",
        "cdi_health.api.server",
        "--host",
        "0.0.0.0",
        "--port",
        "8844",
        "--data-dir",
        "/var/lib/cdi-health",
      ]
    volumes:
      - cdi-api-data:/var/lib/cdi-health
      - /dev:/dev
      - /run/udev:/run/udev:ro

  dashboard:
    build:
      args:
        VITE_CDI_USE_MOCK_DATA: ${VITE_CDI_USE_MOCK_DATA:-0}
    environment:
      CDI_HEALTH_API_TOKEN: ${CDI_HEALTH_API_TOKEN:?Set CDI_HEALTH_API_TOKEN in .env}
